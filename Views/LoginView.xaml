<Window x:Class="SWP.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập - Quản lý kho vật tư" Height="480" Width="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ColorBg}">
    <Grid>
        <Border Background="White" CornerRadius="10" Margin="24" Padding="28"
                BorderBrush="{StaticResource ColorBorder}" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="KHO VẬT TƯ NỘI BỘ" FontSize="18" FontWeight="Bold"
                           HorizontalAlignment="Center" Foreground="{StaticResource ColorPrimary}"/>
                <TextBlock Text="Đăng nhập hệ thống" FontSize="12" Foreground="{StaticResource ColorTextMuted}"
                           HorizontalAlignment="Center" Margin="0,4,0,24"/>

                <TextBlock Text="Tên đăng nhập" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="TxtTenDangNhap" Margin="0,0,0,14"/>

                <TextBlock Text="Mật khẩu" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="TxtMatKhau" Margin="0,0,0,14"/>

                <!-- IsDefault="True" thay cho sự kiện KeyDown riêng: Enter ở ô nào cũng chạy nút này. -->
                <Button Content="ĐĂNG NHẬP" Style="{StaticResource BtnPrimary}" Margin="0,10,0,0"
                        IsDefault="True" Click="BtnDangNhap_Click"/>

                <TextBlock Text="Tài khoản mẫu: thukho1 / ketoan1 / quanly1 — mật khẩu: 123456"
                           FontSize="10" Foreground="{StaticResource ColorTextMuted}"
                           HorizontalAlignment="Center" Margin="0,20,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
