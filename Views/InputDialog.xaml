<Window x:Class="SWP.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nhập thông tin" Height="220" Width="420"
        WindowStartupLocation="CenterScreen" Background="White"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTieuDe" Grid.Row="0" FontSize="15" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock x:Name="TxtGoiY" Grid.Row="1" Foreground="{StaticResource ColorTextMuted}" FontSize="12" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBox x:Name="TxtNoiDung" Grid.Row="2" AcceptsReturn="True" TextWrapping="Wrap" VerticalAlignment="Top" Height="70"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Style="{StaticResource BtnSecondary}" Width="90" Margin="0,0,8,0" Click="BtnHuy_Click"/>
            <Button Content="Xác nhận" Style="{StaticResource BtnPrimary}" Width="90" Click="BtnXacNhan_Click"/>
        </StackPanel>
    </Grid>
</Window>
